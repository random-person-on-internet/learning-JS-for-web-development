const obj = {
  name: "Ved",
  age: 18,
  city: "Ahmedabad",
};

// obj to JSON string
const str = JSON.stringify(obj);
console.log(str);

// JSON string to obj
const obj2 = JSON.parse(str);
console.log(obj2);

// bs atluj chhe JSON ma, jlsa kro :)
